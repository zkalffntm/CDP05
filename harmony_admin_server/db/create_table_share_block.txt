create table share_block (
	m_num1 int,
	b_num1 int,
	m_num2 int,
	b_num2 int,

	primary key(m_num1, b_num1, m_num2, b_num2),
	foreign key(m_num1, b_num1) references block(m_num, b_num),
	foreign key(m_num2, b_num2) references block(m_num, b_num)
);