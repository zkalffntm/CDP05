create table share_block (
	m_num1 int,
	bl_num1 int,
	m_num2 int,
	bl_num2 int,

	primary key(m_num1, bl_num1, m_num2, bl_num2),
	foreign key(m_num1, bl_num1) references block(m_num, bl_num),
	foreign key(m_num2, bl_num2) references block(m_num, bl_num)
);