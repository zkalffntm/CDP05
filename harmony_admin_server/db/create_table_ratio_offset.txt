create table ratio_offset (
	m_num int,
	bl_num int,
	be_minor int,
	ro_ratio float,

	primary key(m_num, bl_num, be_minor),
	foreign key(m_num, bl_num) references block(m_num, bl_num),
	foreign key(be_minor) references beacon(be_minor)
);