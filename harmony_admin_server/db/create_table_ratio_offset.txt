create table ratio_offset (
	m_num int,
	b_num int,
	ro_beacon_minor int
	ro_ratio float,

	primary key(m_num, b_num, ro_beacon_minor),
	foreign key(m_num, b_num) references block(m_num, b_num)
);